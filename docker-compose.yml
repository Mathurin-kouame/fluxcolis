services:

  # ==================
  # FRONTEND (React / Vite)
  frontend:
    build: ./frontend
    container_name: fluxcolis_ui
    restart: unless-stopped
    ports:
      - "5174:5174"
    depends_on:
      - api
    env_file:
      - ./frontend/.env
 
  # ===================
  # BACKEND (NestJS + Prisma)
  api:
    build: ./backend
    container_name: fluxcolis_api
    restart: unless-stopped
    ports:
      - "3000:3000"
      - "5555:5555"
    depends_on:
      - db
    env_file:
      - ./backend/.env

  # ===================
  # DATABASE (PostgreSQL)
  db:
    image: postgres:15
    container_name: fluxcolis_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

# ===================
# VOLUMES
volumes:
  postgres_data: